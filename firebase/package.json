{
  "name": "todo-box-firebase",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "firebase emulators:start --only auth,functions,firestore,storage",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^10.2.2"
  }
}
